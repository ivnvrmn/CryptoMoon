<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rmnivnv.cryptomoon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CMApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.addCoin.AddCoinActivity" />
        <activity android:name=".ui.coinInfo.CoinInfoActivity" />
        <activity android:name=".ui.coinAllocation.CoinAllocationActivity" />
        <activity android:name=".ui.addTransaction.AddTransactionActivity" />
        <activity android:name=".ui.holdings.HoldingsActivity"/>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="4615563d0895ce49806b192cb9f65a94a8f79455" />
    </application>

</manifest>